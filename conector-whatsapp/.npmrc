# O conector usa o Chrome/Edge que já existe no computador.
# Sem isto, o Puppeteer tenta baixar um Chrome próprio (~150 MB) e a
# instalação falha em redes lentas ou quando o download fica pela metade.
puppeteer_skip_download=true
PUPPETEER_SKIP_DOWNLOAD=true
