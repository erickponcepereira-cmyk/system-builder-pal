// Modo manutenção global.
// Enquanto `MAINTENANCE_MODE` for `true`, todo o app exibe a tela de manutenção
// em vez das telas normais (login, loja, painéis), evitando erros de conexão.
// Para voltar ao normal, basta trocar para `false`.
export const MAINTENANCE_MODE = false;

// Previsão de retorno exibida na tela (texto livre).
export const MAINTENANCE_ETA = "12 horas";
